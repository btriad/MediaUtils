# GUI dependencies (tkinter is built-in with Python)

# Core dependencies for media processing
Pillow>=10.0.0
pillow-heif>=0.10.0

# Writing EXIF metadata (used by the EXIF Date Restorer / restore_dates_gui.py)
piexif>=1.1.3

# Latin transliteration of non-English city names (Media File Renamer).
# Greek and Turkish work without it; this adds Cyrillic, Chinese, Japanese, ...
Unidecode>=1.3

# Optional dependencies for enhanced functionality
# typing_extensions>=4.0.0  # For older Python versions (< 3.8)

# External tools (not Python packages)
# FFmpeg/ffprobe - Required for video metadata extraction
# Download from: https://ffmpeg.org/download.html
# Or install via package manager:
#   Windows: choco install ffmpeg
#   macOS: brew install ffmpeg  
#   Linux: sudo apt install ffmpeg